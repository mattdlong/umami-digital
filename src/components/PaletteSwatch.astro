---
interface Props { palette: { name: string; hex: string }[] }
const { palette } = Astro.props;
---
<div class="palette" role="list" aria-label="Colour palette">
  {palette.map(({ name, hex }) => (
    <div class="entry" role="listitem">
      <div class="swatch" style={`background:${hex}`}></div>
      <span class="name">{name}</span>
      <span class="hex">{hex}</span>
    </div>
  ))}
</div>

<style>
  .palette { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 0.75rem; }
  .entry { display: grid; gap: 0.15rem; }
  .swatch { aspect-ratio: 1 / 1; border-radius: 12px; border: 1px solid var(--hairline); margin-bottom: 0.5rem; }
  .name { font-weight: 600; font-size: 0.95rem; }
  .hex { font-size: 0.78rem; color: var(--ink-50); font-variant-numeric: tabular-nums; }
</style>
